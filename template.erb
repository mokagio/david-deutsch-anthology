<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The David Deutsch Anthology</title>
  </head>
  <body>
    <p>This page collects <a href="https://www.daviddeutsch.org.uk/" target="_blank">David Deutsch's</a> books, talks, and interviews.</p>
    <p>You can contribute to the list <a href="https://github.com/mokagio/david-deutsch-anthology" target="_blank">here</a>.</p>
    <h1>Books</h1>
    <ul>
      <% books.each do |book| %>
        <li>
          <strong><%= book['name'] %></strong>
          <br>
          Published on: <%= book['published_date'] %>
        <br>
        <a href="<%= book['url'] %>" target="_blank">More Info</a>
        </li>
      <% end %>
    </ul>

    <h1>Talks</h1>
    <ul>
      <% talks.each do |talk| %>
        <li>
          <strong><%= talk['title'] %></strong>
          <br>
          Delivered on: <%= talk['delivered_date'] %>
        <br>
        <a href="<%= talk['url'] %>" target="_blank">Watch</a>
        </li>
      <% end %>
    </ul>

    <h1>Podcast Interviews</h1>
    <ul>
      <% podcast_interviews.each do |podcast| %>
        <li>
          <strong><%= podcast['title'] %></strong>
          <br>
          Show: <a href="<%= podcast['show_url'] %>" target="_blank"><%= podcast['show'] %></a>
          <br>
          Published on: <%= podcast['published_date'] %>
        <br>
        <a href="<%= podcast['url'] %>" target="_blank">Listen</a>
        </li>
      <% end %>
    </ul>

    <h1>Other</h1>
    <ul>
      <% other.each do |item| %>
        <li>
          <strong><%= item['title'] %></strong>
          <br>
          <%= item['details'] %>
          <br>
          <a href="<%= item['url'] %>" target="_blank">Visit</a>
        </li>
      <% end %>
    </ul>
    <hr/>
    <p>Created by <a href="https://giolodi.com" target="_blank">Gio Lodi</a>.</p>
  </body>
</html>
